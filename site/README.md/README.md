# GEI Site

Public website for GEI.

## Build

Run the site build to copy static files into `dist/`.

## Deploy

Upload the contents of `dist/` to your web host, or configure your host to serve that folder as the site root.