import type { DependencyGraph } from '../graph/DependencyGraph';

export class CycleValidation {
  validate(graph: DependencyGraph): void {
    // Placeholder for cycle detection logic.
    // This will be expanded once we wire in the graph algorithms more fully.
    void graph;
  }
}